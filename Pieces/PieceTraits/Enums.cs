using System;
using System.Collections.Generic;
using System.Text;

namespace ChessCheck
{
    public enum PieceType { King, Rook}
    public enum Color { Black, White}
    public enum GameStatus { Active, Draw, WhiteLose, BlackLose}
}
